namespace NewtonInterpolationSolver
{
    // Клас, що містить два масиви типу double[] та значення інтерполяції
    public class DataArrays
    {
        // Властивість для зберігання значень X
        public double[] XValues { get; set; }

        // Властивість для зберігання значень Y
        public double[] YValues { get; set; }

        // Властивість для зберігання значення інтерполяції
        public double InterpolationValue { get; set; }
    }
}
